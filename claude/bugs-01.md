# Bugs — 01 (ACTIVE)

Error matrix for Panameer. Entries use a stable id (e.g. `E01`, `P1-J1-E01`)
so briefs and handoffs can reference them. This is the ACTIVE file in the
numbered series; freeze at ~2500 lines and start `bugs-02.md`. Read every
`bugs-NN.md` in ascending order for the full matrix.

Newest entry at top.

---

_No bugs logged yet. First entries land once the app has surfaces to walk._

---

> Entry format:
> ### E<NN> — <short title> — <OPEN | FIXED (commit) | WONTFIX>
> - **Where:** route / component / file
> - **Symptom:** what's wrong
> - **Cause:** root cause (verify against diffs — a stated cause can be wrong)
> - **Fix:** what resolved it (commit hash) or the plan
