import { SpecPage } from "@/components/console/SpecPage";

/** Admin console page, built from its 2.5 deck slide via ADMIN_PAGES. */
export default function Page() {
  return <SpecPage slug="work-packages" />;
}
