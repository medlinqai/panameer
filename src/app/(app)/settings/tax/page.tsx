import { ComingSoon } from "@/components/settings/ComingSoon";

export default function TaxPage() {
  return (
    <ComingSoon
      title="Tax Information"
      body="Tax details (W-9 / W-8, VAT, and payout tax forms) will be collected here. We don't store any tax IDs yet — this is a placeholder while we finish secure handling."
    />
  );
}
